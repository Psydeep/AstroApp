let express = require('express');
let router = express.Router();

let mongoose = require('./../config/conexion');
let User = require('./../models/user');
let Documento = require('./../models/documentos');
/* GET home page. */

module.exports = router;
